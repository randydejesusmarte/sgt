name: taller_autos
description: App para gestión de taller de autos
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Dependencias existentes
  flutter_modular: ^6.3.4
  flutter_bloc: ^9.1.1
  equatable: ^2.0.5
  sqflite: ^2.3.0
  path: ^1.8.3
  intl: ^0.20.2
  mask_text_input_formatter: ^2.9.0

  sqflite_common_ffi: ^2.3.6  # Para testing en desktop
  
  # NUEVAS DEPENDENCIAS
  pdf: ^3.10.4              # Para generar PDFs
  printing: ^5.11.0         # Para imprimir y previsualizar PDFs
  path_provider: ^2.1.1     # Para acceder a directorios del sistema

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: 
  uses-material-design: true